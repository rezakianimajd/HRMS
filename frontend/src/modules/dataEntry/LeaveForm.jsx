// NOTE: Leave entry is now handled in the dedicated "مرخصی و مأموریت" page
// (https://.../leaves) with single + bulk-excel entry. This legacy form was
// removed from "ورود اطلاعات" to avoid duplication.
const LeaveForm = () => null;
export default LeaveForm;
